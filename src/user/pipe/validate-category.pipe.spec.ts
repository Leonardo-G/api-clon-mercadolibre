import { ValidateCategoryPipe } from './validate-category.pipe';

describe('ValidateCategoryPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCategoryPipe()).toBeDefined();
  });
});
